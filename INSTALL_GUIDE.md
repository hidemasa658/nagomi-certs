# なごみ株式会社 admin — 証明書 導入 手順書

## URL

**リポジトリ**: https://github.com/hidemasa658/nagomi-certs

## 必要な情報 (別途 対面 or 安全な チャネルで 伝達)

| 項目 | 用途 |
|---|---|
| **7z 解凍 パスワード** | ダウンロード後 の zip 展開 |
| **p12 プロファイル パスワード** | 証明書 インストール時 |

---

## 📱 iPad の 場合

### 事前準備

**App Store で 「iZip」 (無料) を インストール**  
[iZip 検索リンク](https://apps.apple.com/jp/app/izip-zip-unzip-unrar-tool/id413971331)

### Step 1: 7z ダウンロード

1. **Safari** で 開く:
   ```
   https://github.com/hidemasa658/nagomi-certs
   ```
2. **`nagomi-certs.7z`** をタップ
3. 右上の **「Raw」** ボタン (or 「View raw」)
4. **「nagomi-certs.7z」をダウンロード** → **「ダウンロード」**
5. Safari 右上の **↓ (下矢印)** アイコンで ダウンロード完了 確認
6. **「ファイル」アプリ で表示** タップ

### Step 2: 解凍

1. **「ファイル」アプリ** で `nagomi-certs.7z` タップ
2. 「共有」→ **「iZip で開く」**
3. iZip 画面 → **パスワード入力** (7z解凍パスワード)
4. **解凍完了** → `keiei-ipad.p12` などが 展開

### Step 3: プロファイル インストール

1. **`keiei-ipad.p12`** をタップ
2. 「共有」→ **「ファイルに保存」** (iCloud Drive/Downloads)
3. **「ファイル」アプリ** に戻って `.p12` タップ
4. 画面上部 **「プロファイル」を ダウンロード** メッセージ
5. **設定アプリ** を開く → 上部 **「プロファイルがダウンロード済み」** タップ
   - 見当たらない場合: 設定 → 一般 → **VPN と デバイス管理** → プロファイル
6. **「インストール」** → **iPad パスコード 入力**
7. **警告画面** で もう一度 **「インストール」**
8. **証明書パスワード 入力** (p12 パスワード)
9. **右上「完了」**

### Step 4: 信頼設定 (重要)

**設定 → 一般 → 情報 → 証明書信頼設定** → **「Nagomi Pharmacy Root CA」を トラスト** に

### Step 5: Safari で アクセス

```
https://nagomi-admin.duckdns.org/gallery/nagomi-corp/
```

- 「この Web サイトは 証明書を要求しています」表示
- **「keiei-ipad」を選択** → **「続ける」**
- 管理画面 表示 ✅

### Step 6 (任意): ホーム画面 追加

Safari で 管理画面 開いてる状態で:
1. **共有ボタン (□↑)**
2. **「ホーム画面に追加」**
3. 名前: 「なごみ管理」等 → **追加**

---

## 💻 Mac の 場合

### Step 1: 7z ダウンロード

1. **Chrome / Safari** で 開く:
   ```
   https://github.com/hidemasa658/nagomi-certs
   ```
2. **`nagomi-certs.7z`** をクリック
3. 右上の **「Raw」** ボタン
4. ダウンロード → `~/Downloads/nagomi-certs.7z`

### Step 2: 解凍

**方法A**: **Keka** (無料アプリ、7z対応)
1. App Store で **「Keka」** インストール
2. `nagomi-certs.7z` を ダブルクリック
3. **パスワード入力** → 解凍完了

**方法B**: Terminal (7z コマンド)
```bash
# 7z が入ってない場合
brew install sevenzip

# 解凍
cd ~/Downloads
7zz x nagomi-certs.7z
# パスワード入力
```

### Step 3: p12 インストール

1. **`keiei-pc.p12`** を ダブルクリック
2. **キーチェーン**: **「ログイン」** を選択 → **「追加」**
3. **証明書パスワード 入力** (p12 パスワード)
4. **Mac ログインパスワード** 入力 → **「常に許可」**

### Step 4: 信頼設定

1. **キーチェーンアクセス** アプリを開く
2. **「システム」or「ログイン」** キーチェーン内で **「Nagomi Pharmacy Root CA」** 検索
3. ダブルクリック → **「信頼」** タブ 展開
4. **「この証明書を使用するとき」** → **「常に信頼」**
5. パスワード入力 → 保存

### Step 5: Chrome で アクセス

```
https://nagomi-admin.duckdns.org/gallery/nagomi-corp/
```

- **証明書選択ダイアログ** → **「keiei-pc」を選択** → **OK**
- 「Chrome が キーチェーン に アクセス」表示 → **Mac パスワード** → **「常に許可」**
- 管理画面 表示 ✅

---

## 🚨 トラブル

| 症状 | 対処 |
|---|---|
| 「プロファイルが表示されない」(iPad) | 設定 → 一般 → VPN と デバイス管理 → プロファイル |
| 「keiei-ipad が 選択できない」(Safari) | Step 4 の 信頼設定 が 抜けてる |
| 「信頼されていない証明書」(Chrome) | Step 4 の 「常に信頼」設定 |
| 「証明書選択ダイアログが出ない」 | ブラウザ 完全再起動 (全タブ閉じて 起動しなおす) |
| パスワード違います | 大文字小文字・全角/半角 に注意、`0` (ゼロ) と `O` (オー) の 見分け |

## セキュリティ 注意

- **p12 ファイル 単体を LINE 等で 送るのは 禁止** (パスワード付き 7z で 配布のみ)
- パスワードは **口頭 or 対面 メモ** で 別途 伝達
- **紛失 時は 即連絡** (新しい 証明書 発行 + 旧 証明書 失効)
